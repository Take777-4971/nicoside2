# NicoSide プロジェクト引き継ぎサマリー

新しいチャットの最初のメッセージに、このファイルの内容を貼り付けるか
ファイルごとアップロードしてください。あわせて最新の`nicoside_pyweb.zip`
もアップロードすると、そのままソースを見ながら続きを進められます。

## プロジェクト概要

`NicoSide` は、YouTube / ニコニコ動画を検索・再生できる、デスクトップの
端に常駐させて使う「サイドバー型ガジェット」アプリ。

**技術スタック**: Python + [pywebview](https://pywebview.flowrl.com/)
（Windowsでは内部的にEdge WebView2を使用）。UIはHTML/CSS/JSで書かれ、
`js_api`経由でPythonとブリッジしている。

### なぜFletではなくpywebviewなのか
最初はFletで実装していたが、FletのWebViewコントロールがWindows/Linux
デスクトップで未対応と判明。動画埋め込み再生・常時最前面表示・
コンパクトなガジェット的UIを両立させるため、pywebviewに全面移行した。

## ファイル構成

```
nicoside_pyweb/
├── main.py             # ウィンドウ起動処理
├── api.py              # JS(UI) <-> Python ブリッジ（js_api）。検索・ページング・
│                        # ウィンドウ操作（常時最前面・サイズ・自動的に隠す・端スナップ）を担当
├── auth.py              # YouTube OAuth 2.0（InstalledAppFlow）
├── providers/
│   ├── base.py           # プロバイダ共通インターフェース（ページング対応）
│   ├── niconico.py        # ニコニコ動画スナップショット検索API（APIキー不要）
│   └── youtube.py         # YouTube Data API v3（OAuthアクセストークンで呼び出す）
├── ui/
│   ├── index.html
│   ├── style.css
│   └── app.js
├── requirements.txt
└── README.md            # これまでの全修正内容・検証内容の詳細ログ
```

`README.md`に、これまでの全てのやり取りでの修正内容・技術的な理由・
検証済みの内容・実機でしか確認できていない内容が時系列でまとめて
あります。詳細はそちらを参照してください。

## 現在実装済みの機能

- ニコニコ動画・YouTubeの検索（キーワード検索、5件程度ずつページング、
  最大100件まで）
- YouTube再生リストの選択・表示（ログイン中アカウント自身のリスト。
  編集は非対応）
- 動画の埋め込み再生（`<iframe>`、`http://127.0.0.1:port/`経由で配信
  することでYouTube埋め込みが正常動作するようにしている）
- YouTube OAuth 2.0ログイン（APIキー不要。`client_secret.json`を
  開発者側で用意する必要あり）
- リピート再生: 1曲リピート（YouTubeのみ、URLパラメータ方式） / 
  リストリピート（YouTube IFrame Player APIで終了検知、現在表示中の
  ページ内を順送り）
- ウィンドウのピン留め: OFF / 常に最前面 / 自動的に隠す（画面端に
  「にゅるっと」畳まれ、マウスが近づくと戻る）の3モード
- ウィンドウサイズ選択（x1/x2/x3）。常にディスプレイの高さいっぱい・
  y=0で、幅だけが変わる「ガジェットらしい」サイズ
- ウィンドウをドラッグして画面端に近づけるとスナップする
- 動画プレイヤーの高さは、実際の描画幅から16:9になるよう自動計算

## 直近の修正（このチャットの最後の対応）

「自動的に隠す」モードで畳む→展開を繰り返すと、ウィンドウ位置が
右方向に少しずつズレていき、最終的に画面外に一部がはみ出す不具合を
修正した。原因は、アンカー座標をウィンドウ自身の自己申告値
（`window.x`など）から毎回計算していたため、実機での微小な誤差が
サイクルごとに蓄積していたこと。修正後は、ディスプレイの絶対座標
（`screen.x`など）を基準にするよう変更し、意図的に誤差を再現した
モックでの5サイクル連続テストで、位置がズレずに安定することを確認済み。

**この修正はまだ実機で確認できていません。** 次のチャットでまず
最優先で確認してほしい項目です。

## 実機でまだ確認できていないこと（優先度高い順）

1. **「自動的に隠す」モードの座標ズレ修正**（上記、直近の修正）
2. リストリピートで、実際のYouTube動画において終了検知・自動送りが
   機能するか（IFrame Player APIスクリプトがこのサンドボックス環境
   からは読み込めず、モックでのロジック検証のみ済み）
3. OAuthログインフローが実機で問題なく完了するか、`token.json`が
   正しく保存・再利用されるか
4. ウィンドウの端スナップの実際の使用感（ロジックはモックで検証済み）
5. 「自動的に隠す」の「にゅるっと」感（アニメーションの滑らかさ・
   タイミング）が期待通りか

## 相談・保留中の話題

- **一般公開時のOAuthの注意点**（既に一度回答済みだが、次のチャットでも
  参照される可能性がある）:
  - YouTube Data APIのクォータ（デフォルト1日10,000ユニット）は
    **開発者のGoogle Cloudプロジェクト単位**。ユーザー数が増えても
    ユーザーごとに別枠になるわけではない
  - OAuth同意画面が「テスト」ステータスのままだと、リフレッシュ
    トークンが7日で失効し、ユーザーは定期的な再ログインが必要になる
  - 「本番」ステータスへの切り替えには、スコープの内容によっては
    Googleの審査（アプリ確認）が必要になる場合がある
  - `client_secret.json`自体はGoogleの見解では「完全な機密情報」では
    ないが、リポジトリ等での安易な公開は避けるべき

- **将来のエンハンス**（まだ調査段階、実装はしていない）: 検索結果・
  再生リスト・マイリストをリストとして選択し、全曲/1曲ループを
  制御できるような、より本格的な連続再生機能。現状のリストリピートは
  「表示中の1ページ分のみ」が対象という制限がある。複数ページ・
  最大100件全体をまたぐシームレスな連続再生には、ページ単位ではない
  「キュー」のような別のデータ構造が必要になる見込み。

## 次のチャットで最初にやるべきこと（提案）

1. 上記「直近の修正」（自動的に隠すモードの座標ズレ）が実機で
   解消しているか確認
2. 問題なければ、実機でのその他の未確認項目（リストリピート・OAuth
   フロー・端スナップ）を一通り試してもらう
3. 問題が見つかれば都度修正
